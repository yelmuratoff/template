name: database
description: Persistence infrastructure — Drift executors, typed preferences DAO, secure storage.
version: 0.0.1
publish_to: "none"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  drift: ^2.19.1
  sqlite3_flutter_libs: ^0.6.0+eol
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^10.0.0
  path: ^1.9.0
  path_provider: ^2.1.4
  ispect: 5.2.0-dev.19

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
